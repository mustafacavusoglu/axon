name: ner_pipeline
platform: script
max_batch_size: 1

inputs:
  - name: text
    data_type: TYPE_STRING
    dims: [1]

outputs:
  - name: entities
    data_type: TYPE_STRING
    dims: [1]

instance_groups:
  - count: 2
    kind: KIND_CPU
