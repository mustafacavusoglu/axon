name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  IMAGE_NAME: mustafa12/axon

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      src: ${{ steps.filter.outputs.src }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            src:
              - 'inference-engine/**'
              - 'Dockerfile.inference-engine'
              - 'docker-entrypoint.sh'
              - '.github/workflows/ci.yml'

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            inference-engine/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('inference-engine/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends protobuf-compiler libssl-dev

      - name: cargo fmt --check
        run: cargo fmt --check
        working-directory: inference-engine

      - name: cargo clippy
        run: cargo clippy -- -D warnings
        working-directory: inference-engine

      - name: cargo test
        run: cargo test --bins
        working-directory: inference-engine


  docker:
    needs: [changes, test]
    if: |
      needs.test.result == 'success'
      && needs.changes.outputs.src == 'true'
      && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment: docker-env
    strategy:
      matrix:
        platform:
          - linux/amd64
          # ARM64 built locally (push-arm64.sh)
          # - linux/arm64
    steps:
      - uses: actions/checkout@v4

      # ARM64 built locally, no QEMU needed
      # - name: Set up QEMU
      #   uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Extract version
        id: ver
        run: |
          VERSION=$(grep '^version' inference-engine/Cargo.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Set arch suffix
        id: arch
        run: |
          if [[ "${{ matrix.platform }}" == "linux/amd64" ]]; then
            echo "suffix=amd64" >> $GITHUB_OUTPUT
          else
            echo "suffix=arm64" >> $GITHUB_OUTPUT
          fi

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.inference-engine
          platforms: ${{ matrix.platform }}
          push: true
          tags: ${{ env.IMAGE_NAME }}:${{ steps.ver.outputs.version }}-${{ steps.arch.outputs.suffix }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test AMD64 ONNX loading
        if: matrix.platform == 'linux/amd64'
        shell: bash
        run: | 
          set -euo pipefail
          IMAGE="${{ env.IMAGE_NAME }}:${{ steps.ver.outputs.version }}-${{ steps.arch.outputs.suffix }}"
          cleanup() { docker rm -f axon-smoke >/dev/null 2>&1 || true; }
          trap cleanup EXIT

          docker run -d --name axon-smoke \
            -v "$PWD/models/test_model:/models/test_model:ro" \
            -p 18000:8000 \
            "$IMAGE"

          for i in {1..10}; do
            if curl -sf http://localhost:18000/v2/health/ready >/dev/null; then
              echo "Server ready, health check passed"
              exit 0
            fi
            echo "[$i/10] Waiting for server to be ready..."
            sleep 2
          done

          echo "=== Server logs ==="
          docker logs axon-smoke
          echo "Health check failed after 10 attempts" >&2
          exit 1

  docker-manifest:
    needs: docker
    runs-on: ubuntu-latest
    environment: docker-env
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Extract version
        id: ver
        run: |
          VERSION=$(grep '^version' inference-engine/Cargo.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create and push multi-arch manifest
        run: |
          VERSION="${{ steps.ver.outputs.version }}"
          IMAGE="${{ env.IMAGE_NAME }}"
          docker buildx imagetools create -t ${IMAGE}:${VERSION} \
            ${IMAGE}:${VERSION}-amd64 \
            ${IMAGE}:${VERSION}-arm64
          docker buildx imagetools create -t ${IMAGE}:latest \
            ${IMAGE}:${VERSION}-amd64 \
            ${IMAGE}:${VERSION}-arm64

  release-build:
    needs: [changes, test]
    if: |
      needs.test.result == 'success'
      && needs.changes.outputs.src == 'true'
      && github.event_name == 'push'
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            suffix: amd64
          # ARM64 built locally (push-arm64.sh) — QEMU too slow in CI
          # - target: aarch64-unknown-linux-gnu
          #   os: ubuntu-24.04-arm
          #   suffix: arm64
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends protobuf-compiler libssl-dev

      - name: Build release binary
        run: cargo build --release --target ${{ matrix.target }}
        working-directory: inference-engine

      - name: Rename binary
        run: |
          mv target/${{ matrix.target }}/release/axon-server axon-server-linux-${{ matrix.suffix }}
        working-directory: inference-engine

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: binary-${{ matrix.suffix }}
          path: inference-engine/axon-server-linux-${{ matrix.suffix }}

  release:
    needs: [release-build]
    runs-on: ubuntu-latest
    environment: docker-env
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: binary-*
          merge-multiple: true
          path: release

      - name: Extract version
        id: ver
        run: |
          VERSION=$(grep '^version' inference-engine/Cargo.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.ver.outputs.version }}
          name: v${{ steps.ver.outputs.version }}
          files: |
            release/axon-server-linux-amd64
            # ARM64 binary built locally (push-arm64.sh)
            # release/axon-server-linux-arm64
          generate_release_notes: true
