apiVersion: v1
kind: Service
metadata:
  name: inference-server
spec:
  type: NodePort
  selector:
    app: inference-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
    - name: grpc
      port: 8001
      targetPort: 8001
